/** enum for density types */
export enum Density {
  /**
   * Relaxed density (more spacing)
   */
  RELAXED = "theme-relaxed",

  /**
   * Condensed density (less spacing)
   */
  CONDENSED = "theme-condensed",
}
