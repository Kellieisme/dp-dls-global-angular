import { Meta, Markdown, Canvas } from "@storybook/addon-docs/blocks";
import * as NavigationStories from "./navigationDrawer.stories";

<Meta of={NavigationStories} />

<div style={{backgroundColor:'var(--color-ui-background-medium)',padding:'8px 15px', borderRadius: '8px'}}>

# Navigation Drawer

The `NavigationDrawerComponent` is a versatile sidebar navigation component built using Angular Material. It provides a collapsible and expandable navigation drawer that can be presented in different styles, including default, standalone, and modal. The component supports dynamic menu items, allowing for a structured navigation experience within applications.

### Selectors

- `ba-navigation-drawer`: The main selector for the navigation drawer component.

### Usage

In your typescript file.

The selector, templateUrl, styleUrls, and class name (NavigationDrawerPageComponent) can be different based on your project structure and naming conventions.

```typescript
import { Component } from '@angular/core';
import { NavigationDrawerComponent } from '@jeppesen-foreflight/dp-dls-global-angular/navigation-drawer';
import { UserProfileComponent } from '@jeppesen-foreflight/dp-dls-global-angular/user-profile';
import { MatMenuModule } from '@angular/material/menu';
import { MatDividerModule } from '@angular/material/divider';
import { IconRegistryModule } from '@jeppesen-foreflight/dp-dls-global-angular/icon-registry';
import { MatIconModule } from '@angular/material/icon';
import { MatButtonModule } from '@angular/material/button';

@Component({
  selector: 'navigation-drawer-page',
  standalone: true,
  imports: [NavigationDrawerComponent, UserProfileComponent, MatMenuModule, MatButtonModule, MatDividerModule, IconRegistryModule, MatIconModule],
  templateUrl: './navigation-drawer-page.component.html',
  styleUrl: './navigation-drawer-page.component.scss'
})
export class NavigationDrawerPageComponent {

}
```

## API reference

## Features

- Customizable branding (app name & logo)
- Supports different presentation styles: `Default`, `Standalone`, and `Modal`
- Configurable expansion and collapse behavior
- Slot for user profile and notifications button

Important Note on the opened Property
The opened property of the NavigationDrawerComponent is used to control the visibility of the navigation drawer. To effectively manage this property, you should implement two-way data binding. This allows the parent component to update the opened state based on user interactions, including clicks on the backdrop.

Two-Way Binding Example
To enable two-way binding for the opened property, you can use the following syntax in your template:

```html
<ba-navigation-drawer
  [appLogo]="'my-logo'"
  [appName]="'My Application'"
  [homeRoute]="'/'"
  [navDrawerMenuItems]="myMenuItems"
  [navDrawerPresentation]="'standalone'"
  [(opened)]="opened"> <!-- Two-way binding for opened state -->
</ba-navigation-drawer>
```

### Properties

<Markdown>
{`
| Name                                              | Description                                                                                                             |
|---------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------|
| @Input() appLogo                                  | The application logo, used when the Nav Drawer is rendered in its STANDALONE configuration.                             |
| @Input() appName                                  | The application name, used when the Nav Drawer is rendered in its STANDALONE configuration.                             |
| @Input() homeRoute                                | Application home route, will be added as a routerLink when clicking the logo.                                           |
| @Input() collapsedWidth                           | Sets the width for the drawer when in its collapsed state.                                                              |
| @Input() expandedWidth                            | Sets the width for the drawer when in its expanded state.                                                               |
| @Input() fixedOpen                                | An input flag denoting whether or not the Nav Drawer should never collapse down to a minified state.                    |
| @Input() minifyOnCollapse                         | An input flag denoting whether or not the Nav Drawer should show a minified presentation when collapsed.                |
| @Input() navBarBottomComponent1                   | Template for the first functional component aligned to the bottom of the drawer.                                        |
| @Input() navBarBottomComponent2                   | Template for the second functional component aligned to the bottom of the drawer.                                       |
| @Input() navBarBottomComponent3                   | Template for the third functional component aligned to the bottom of the drawer.                                        |
| @Input() navBarBottomComponent4                   | Template for the fourth functional component aligned to the bottom of the drawer.                                       |
| @Input() navDrawerMenuItems                       | Menu items for the navdrawer.                                                                                           |
| @Input() navDrawerPresentation: "default"         | An input flag denoting the presentation style of the Nav Drawer. Acceptable values are: "default", "standalone", "modal".|
| @Input() opened                                   | A direct map to Angular Material's opened property of the Side Nav component.                                           |
`}
</Markdown>

### Examples
You can see how the different inputs work in the individual component stories.

#### Default NavigationDrawer Representation
<Canvas of={NavigationStories.Default} />

#### Standalone NavigationDrawer Representation
<Canvas of={NavigationStories.Standalone}/>

#### Modal NavigationDrawer Representation
<Canvas of={NavigationStories.Modal}/>

#### FixedOpen NavigationDrawer Representation
<Canvas of={NavigationStories.FixedOpen}/>

You can find the usage guidelines on [Doc Hub](https://design.pages.boeing.com/design-system/dls-global-docs/components/navigation-drawer)

</div>

<style>
  {`
    .sbdocs{
      background: var(--color-ui-background-lowest);
      color: var(--color-ui-textandicon-high);
    }
    .css-qa4clq :where(p:not(.sb-anchor, .sb-unstyled, .sb-unstyled p)),
    .css-qa4clq :where(li:not(.sb-anchor, .sb-unstyled, .sb-unstyled li)) {
      color: var(--color-ui-textandicon-medium) !important;
    }

    .css-qa4clq :where(h4:not(.sb-anchor, .sb-unstyled, .sb-unstyled h4)),
    .css-qa4clq :where(h3:not(.sb-anchor, .sb-unstyled, .sb-unstyled h3)),
    .css-qa4clq :where(h2:not(.sb-anchor, .sb-unstyled, .sb-unstyled h2)),
    .css-qa4clq :where(h1:not(.sb-anchor, .sb-unstyled, .sb-unstyled h1)){
      color: var(--color-ui-textandicon-high) !important;
    }
    .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr th{
      color: var(--color-ui-textandicon-high);
      background: var(--color-ui-background-medium);
      border: 1px solid var(--color-ui-divider-flat-high);
      
    }
    .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr:nth-of-type(2n),
    .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr td {
      color: var(--color-ui-textandicon-medium);
      background: var(--color-ui-background-low);
      border: 1px solid var(--color-ui-divider-flat-high);

    }
  `}
</style>
