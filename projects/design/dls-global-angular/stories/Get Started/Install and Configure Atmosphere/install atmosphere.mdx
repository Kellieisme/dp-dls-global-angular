import { Meta, Markdown } from "@storybook/blocks";
import {
  Highlight_yellow,
  Blue_text,
  Red_text,
  Deprecated,
  Inprogress,
  Info_Blue_text,
  Info_Section,
} from "../../../mdx_styles.mdx";

<Meta title="INSTALLATION/Install Atmosphere" />
<div style={{backgroundColor:'var(--color-ui-background-low)',padding:'8px 15px', borderRadius: '8px'}}>

  # Install Atmosphere Packages

  Atmosphere has two main NPM packages that contain the Atmosphere tokens and the standard and custom Angular components.

  > <Red_text> **Note**: Atmosphere supports Angular 17 and 18, with separate global Angular packages for each version. If you use an earlier version of Angular, it might not be supported.</Red_text>

  ## Before you start

  - If you don't have an Angular project, [set one up](?path=/docs/installation-create-an-angular-project--overview)
  - If you have a Angular project , make sure it is 17 or above to ensure compatiblility with Atmosphere.

  ## Get Angular Material

  Atmosphere component library is based on Angular Material , therefore make sure that you have it added to your project before setting up Atmosphere.

  If you do not have angular material, run the below code in your terminal.

  ```cmd
  ng add @angular/material
  ```

  ## Setting Up .npmrc file

  You can access the Atmosphere libraries through the <Info_Blue_text>Jeppesen Github package registry</Info_Blue_text> but you must set up your .npmrc files to allow npm to access the package registry.

  Please be connected to your Jeppesen VPN.

  <div style={{background:"var(--color-ui-background-high)",padding:'8px', borderRadius: '8px',margin:'8px'}}>
    <details>
      <summary style={{fontSize:'17px',cursor: 'pointer'}}>
        > #### For Github package registry

      </summary>

      <p>
        If you don't already have a .npmrc file , create one in the root of your angular project ( same level as package.json ).
        The contents of the .npmrc should include:
      </p>

      ```json
      @dasdigitalplatform:registry=https://npm.pkg.github.com/
      //npm.pkg.github.com/:_authToken=GITHUB_AUTH_TOKEN
      ```

      ### Replace the following values:

      <Markdown>
        {`
| Value             | Description                                                                                  |
| ----------------- | -------------------------------------------------------------------------------------------- |
| GITHUB_AUTH_TOKEN |Create a [Github Personal Access Token](https://gitlab-ext.digitalaviationservices.com/-/user_settings/personal_access_tokens) with all permissions. Save in a safe place. Use that string as the GITHUB_AUTH_TOKEN.|
`}
      </Markdown>

    </details>
  </div>

  ## Install your packages

  <Blue_text> After you set up your .npmrc file, you can install the Atmosphere packages.</Blue_text>

  Find the packages below:

  <div style={{background:"var(--color-ui-background-high)",padding:'8px', borderRadius: '8px',margin:'8px'}}>
    <details>

      <summary style={{ fontSize: "17px", cursor: "pointer" }}>
        > #### Packages for Angular 17
      </summary>
      To install the packages for Angular 17, run the following command:

      ```cmd
      npm install @dasdigitalplatform/dls-global-angular@1.0.1 --legacy-peer-deps
      npm install @dasdigitalplatform/dls-global-assets@1.0.0 --legacy-peer-deps
      ```

    </details>
  </div>

  <div style={{background:"var(--color-ui-background-high)",padding:'8px', borderRadius: '8px',margin:'8px'}}>
    <details>

      <summary style={{ fontSize: "17px", cursor: "pointer" }}>
        > #### Packages for Angular 18
      </summary>

      To install the packages for Angular 18, run the following command:

      ```cmd
      npm install @dasdigitalplatform/dls-global-angular@2.7.1 --legacy-peer-deps
      npm install @dasdigitalplatform/dls-global-assets@2.1.2-beta --legacy-peer-deps
      ```

    </details>
  </div>
  <div style={{background:"var(--color-ui-background-high)",padding:'8px', borderRadius: '8px',margin:'8px'}}>
    <details>
      <summary style={{ fontSize: "17px", cursor: "pointer" }}>
        > #### Packages for Angular 19
      </summary>

      To install the packages for Angular 19, run the following command:

      ```cmd
      npm install @dasdigitalplatform/dls-global-angular@3.2.3 --legacy-peer-deps
      npm install @dasdigitalplatform/dls-global-assets@2.0.0 --legacy-peer-deps
      ```

    </details>
  </div>

  <div style={{background:"var(--color-ui-background-high)",padding:'8px', borderRadius: '8px',margin:'8px'}}>
    <details>
      <summary style={{ fontSize: "17px", cursor: "pointer" }}>
        > #### Packages for Angular 20
      </summary>

      To install the packages for Angular 20, run the following command:

      ```cmd
      npm install @dasdigitalplatform/dls-global-angular@4.0.0 --legacy-peer-deps
      npm install @dasdigitalplatform/dls-global-assets@2.0.0 --legacy-peer-deps
      ```

    </details>
  </div>

  > **Note**: If NPM can't connect to Artifactory and the install fails, try the following command:
  >
  > ```cmd
  > npm config set strict-ssl false
  > ```

  <Info_Blue_text>
    After the installation you should find a `@dasdigitalplatform` folder in your `node_modules` folder with both `dls-global-angular` and `dls-global-assets`
  </Info_Blue_text>

  Almost done just a few more steps ...

  > ## Up Next: Configurations for Atmosphere
  >
  > Now we need to [Set Global Styles and Fonts](?path=/docs/installation-setting-global-styles-and-fonts--overview)

</div>

<style>
  {`
    .sbdocs{
      background: var(--color-ui-background-lowest);
      color: var(--color-ui-textandicon-high);
    }
    .css-qa4clq :where(p:not(.sb-anchor, .sb-unstyled, .sb-unstyled p)),
    .css-qa4clq :where(li:not(.sb-anchor, .sb-unstyled, .sb-unstyled li)) {
      color: var(--color-ui-textandicon-medium) !important;
    }

      .css-qa4clq :where(h4:not(.sb-anchor, .sb-unstyled, .sb-unstyled h4)),
      .css-qa4clq :where(h3:not(.sb-anchor, .sb-unstyled, .sb-unstyled h3)),
      .css-qa4clq :where(h2:not(.sb-anchor, .sb-unstyled, .sb-unstyled h2)),
      .css-qa4clq :where(h1:not(.sb-anchor, .sb-unstyled, .sb-unstyled h1)){
        color: var(--color-ui-textandicon-high) !important;
      }
      .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr th{
        color: var(--color-ui-textandicon-high);
        background: var(--color-ui-background-medium);
        border: 1px solid var(--color-ui-divider-flat-high);

      }
      .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr:nth-of-type(2n),
      .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr td {
        color: var(--color-ui-textandicon-medium);
        background: var(--color-ui-background-low);
        border: 1px solid var(--color-ui-divider-flat-high);

      }
`}
</style>
