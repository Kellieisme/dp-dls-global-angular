import { Meta, Canvas, Story } from "@storybook/blocks";
import {
  Blue_text,
  Red_text,
  Info_Blue_text,
  Info_Section,
  NoData,
} from "../../../mdx_styles.mdx";

<Meta title="FOUNDATION/Custom Components" />

<div style={{backgroundColor:'var(--color-ui-background-low)',padding:'8px 15px', borderRadius: '8px'}}>

<NoData></NoData>
{/*
# Using Custom Atmosphere Components

> If the stylesheets are included in the angular.json as per the [configuration instructions](?path=/docs/get-started-install-configure-atmosphere-setting-global-styles-and-fonts--docs), the components will be styled without any further action needed.

> The theme-ing for atmosphere components are completely handled internally by the Library.
> You can get a deeper Understanding of how to use Atmosphere Themes and Tokens [here]().
> To set up the toggle option for light and dark mode you can checkout [Light/Dark Toggle Module]().

<br />
<Blue_text>
  {" "}
  All material components can be found under ATMOSPHERE ANGULAR COMPONENTS >
  COMPONENTS > CUSTOM{" "}
</Blue_text>

<p>Let's try out an example</p>

Create a component for BreadCrumbsOverViewExample. You can use the below code for the `BreadCrumbsOverviewExample.ts` file.
Call the respected html selector in `app.component.ts` to display it in your browser.

BreadCrumbs-Overview-example

Run the following to start your server and see the Atmosphere BreadCrumbs Component:

````cmd
ng serve -o
``` */}


</div>
````
