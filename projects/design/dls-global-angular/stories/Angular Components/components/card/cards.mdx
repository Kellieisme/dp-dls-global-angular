import { Meta, Markdown, Canvas } from '@storybook/blocks';
import * as CardStories from './cards.stories';
import { Alert } from '@storybook/blocks';

<Meta of={CardStories}/>

<div style={{backgroundColor:'var(--color-ui-background-medium)',padding:'8px 15px', borderRadius: '8px'}}>

# Card

### API reference

The Card is a direct implementation of the Angular Material Card component. Custom styles are applied using the Angular Material theming API and additional CSS rules.

### Selectors

`<mat-card>`

### Usage

In your typescript file.

```typescript
import { ChangeDetectionStrategy, Component} from '@angular/core';
import { MatCardModule} from '@angular/material/card';

/**
 * @title Basic cards
 */
@Component({
  selector: "card-overview-example",
  templateUrl: "card-overview-example.html",
  standalone: true,
  imports: [MatCardModule],
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class CardOverviewExample {}
```

{/* <Markdown>
  <div style={{color: 'red', fontWeight: 'bold', padding: '8px', border: '1px solid red', borderRadius: '4px', backgroundColor: 'rgba(255, 0, 0, 0.1)'}}>
  {`
  # **Import_Note:**
  The 'mat-card', 'mat-card-header', 'mat-card-footer' components support **light and dark modes** only when used with components provided by the **Atmosphere Library**.
  If you integrate **third-party components* or use **Angular Material Components** that are **not explicitly supported** by Atmosphere, we **cannot guarantee** proper 
  compatibility with the Atmosphere Design System, including theming, styling & responsivness.

Before using any additional component, please verify their compatibility with the Atmosphere Library to ensure a seamless user experience.
[Doc Hub/Atmosphere Components](https://design.pages.boeing.com/design-system/dls-global-docs/components)
`}

  </div>
</Markdown> */}

### **Important Note:**
 
> The 'mat-card', 'mat-card-header', 'mat-card-footer' components support **light and dark modes** only when used with components provided by the **Atmosphere Library**. If you integrate **third-party components** or use **Angular Material Components** that are **not explicitly supported** by Atmosphere, we **cannot guarantee** proper compatibility with the Atmosphere Design System, including theming, styling & responsiveness.
>
> Before using any additional component, please verify their compatibility with the Atmosphere Library to ensure a seamless user experience.
> [Doc Hub/Atmosphere Components](https://design.pages.boeing.com/design-system/dls-global-docs/components)

## Directives

### MatCard

Atmosphere Design card component. Cards contain content and actions about a single subject. See
(https://design.pages.boeing.com/design-system/dls-global-docs/components/card)

MatCard provides no behaviors, instead serving as a purely visual treatment.

- **Selector**: `mat-card`
- **Exported as**: `matCard`

#### Properties

<Markdown>
{`
| Name                                                       | Description                                                                                  |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| @Input() appearance: MatCardAppearance                     | type MatCardAppearance =  \`outlined\` or \`raised\`|
`}
</Markdown>

### MatCardTitle

Title of card, intended for use within ``<mat-card>``.This component is an optional convenience for one variety of card title any custom title element may be used in its place.

MatCardTitle provides no behaviors, instead serving as a purely visual treatment

- **Selector**: `mat-card-title [mat-card-title] [matCardTitle]

### MatCardSubtitle

Sub-title of a card, intended for use within ``<mat-card>`` beneath a `<mat-card-title>`. This component is an optional convenience for use with other convenience elements, such as `<mat-card-title>`.

MatCardSubtitle provides no behaviors, instead serving as a purely visual treatment.

- **Selector**: `mat-card-subtitle [mat-card-subtitle] [matCardSubtitle]`

### MatCardHeader

Header region of a card, intended for use within `<mat-card>`. This header captures a card title, subtitle, and avatar. This component is an optional convenience for use with other convenience elements, such as `<mat-card-footer>`; any custom header block element may be used in its place.

MatCardHeader provides no behaviors, instead serving as a purely visual treatment.

- **Selector**: `mat-card-header`

### MatCardContent

Content of a card, intended for use within `<mat-card>`. This component is an optional convenience for use with other convenience elements, such as `<mat-card-title>`; any custom content block element may be used in its place.

MatCardContent provides no behaviors, instead serving as a purely visual treatment.

- **Selector**: `mat-card-content`

### MatCardFooter

Footer area a card, intended for use within `<mat-card>`. This component is an optional convenience for use with other convenience elements, such as `<mat-card-content>`; any custom footer block element may be used in its place.

MatCardFooter provides no behaviors, instead serving as a purely visual treatment.

- **Selector**: `mat-card-footer`

### MatCardActions

Bottom area of a card that contains action buttons, intended for use within `<mat-card>`. This component is an optional convenience for use with other convenience elements, such as `<mat-card-content>`; any custom action block element may be used in its place.

MatCardActions provides no behaviors, instead serving as a purely visual treatment.

- **Selector**: `mat-card-actions`
- **Exported as: `matCardActions`

#### Properties

<Markdown>
{`
| Name                                                       | Description                                                                                  |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| @Input() align: \`start\` or \`end\`  | Position of the actions inside the card. |
`}
</Markdown>

### MatCardImage

Primary image content for a card, intended for use within `<mat-card>`. Can be applied to any media element, such as `<img>` or `<picture>`.

This component is an optional convenience for use with other convenience elements, such as `<mat-card-content>`; any custom media element may be used in its place.

MatCardImage provides no behaviors, instead serving as a purely visual treatment.

- **Selector**: `[mat-card-image] [matCardImage]`

## Type Aliases

### `MatCardAppearance`

```ts
type MatCardAppearance = 'outlined' | 'raised';
```

## Additional documentation

Setting `appearance="outlined"` produces the "glass" style card.

<Markdown>
  {`
| Class added to host element                  | effect
| -------------------------------------------- | ------------------------------------------------------------------------ |
| mat-mdc-card--landscape                      | give the Card a landscape layout                                         |
| mat-mdc-card--landscape-sm                   | give the Card a landscape layout at sm breakpoint                        |
| mat-mdc-card--landscape-md                   | give the Card a landscape layout at md breakpoint                        |
| mat-mdc-card--landscape-lg                   | give the Card a landscape layout at lg breakpoint                         |
| mat-mdc-card--landscape-xl                   | give the Card a landscape layout at xl breakpoint                        |
| mat-mdc-card--landscape-xxl                  | give the Card a landscape layout at xxl breakpoint                       |
`}
</Markdown>

## Examples

### Basic Card
<Canvas of={CardStories.SimpleCards}/>

#### Cards with Action Buttons
<Canvas of={CardStories.CardWithActionButtons}/>

#### Card With Footer & Chips
<Canvas of={CardStories.CardWithFooterStory}/>

#### Cards(Interactive): clickable entire card
<Canvas of={CardStories.InteractiveClickableCard}/>

#### Card: Standard Layout with Image
Standard Layout : This card displays a standard layout that includes an image, a title, and content text. It is designed for general use cases where a simple and straightforward presentation of information is required.
<Canvas of={CardStories.CardWithStandardLayoutImage}/>

#### Card(Interactive): Landscape Layoute with Image
Landscape Layout : This card features a landscape orientation. The layout is enhanced by the custom class `mat-mdc-card--landscape`, which adjusts the card's styling to better fit landscape-oriented images.
<Canvas of={CardStories.CardWithLandscapeLayout}/>


## Additional Resources

#### Angular documentation

<Markdown>
  {`
| Boeing DLS Global Angular Library Version    | Angular Material Version  | API Reference                                                                              |
| -------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------ |
| 1.x.x                                        | 17.x.x                    | [Card](https://v17.material.angular.io/components/card/api)                                |
| 2.x.x                                        | 18.x.x                    | [Card](https://v18.material.angular.io/components/card/api)                                |
| 3.x.x                                        | 19.x.x                    | [Card](https://v19.material.angular.io/components/card/api)                                |
`}
</Markdown>

#### Guidelines and Specs

You can find the usage guidelines on [Doc Hub](https://design.pages.boeing.com/design-system/dls-global-docs/components/card)

</div>

<style>
  {`
    .sbdocs{
      background: var(--color-ui-background-lowest);
      color: var(--color-ui-textandicon-high);
    }
    .css-qa4clq :where(p:not(.sb-anchor, .sb-unstyled, .sb-unstyled p)),
    .css-qa4clq :where(li:not(.sb-anchor, .sb-unstyled, .sb-unstyled li)) {
      color: var(--color-ui-textandicon-medium) !important;
    }

    .css-qa4clq :where(h4:not(.sb-anchor, .sb-unstyled, .sb-unstyled h4)),
    .css-qa4clq :where(h3:not(.sb-anchor, .sb-unstyled, .sb-unstyled h3)),
    .css-qa4clq :where(h2:not(.sb-anchor, .sb-unstyled, .sb-unstyled h2)),
    .css-qa4clq :where(h1:not(.sb-anchor, .sb-unstyled, .sb-unstyled h1)){
      color: var(--color-ui-textandicon-high) !important;
    }
    .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr th{
      color: var(--color-ui-textandicon-high);
      background: var(--color-ui-background-medium);
      border: 1px solid var(--color-ui-divider-flat-high);
      
    }
    .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr:nth-of-type(2n),
    .css-qa4clq :where(table:not(.sb-anchor, .sb-unstyled, .sb-unstyled table)) tr td {
      color: var(--color-ui-textandicon-medium);
      background: var(--color-ui-background-low);
      border: 1px solid var(--color-ui-divider-flat-high);

    }
  `}
</style>
