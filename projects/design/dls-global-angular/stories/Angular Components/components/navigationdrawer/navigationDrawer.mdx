import { Meta, Markdown } from '@storybook/blocks';
import * as NavigationStories from './navigationDrawer.stories';

<Meta title="Overview" of={NavigationStories} />

# Navigation Drawer

The `NavigationDrawerComponent` provides a side navigation experience with customizable branding, menu items, and collapsible behavior.

## API reference

## Features
- Customizable branding (app name & logo)
- Supports different presentation styles: `Default`, `Standalone`, and `Modal`
- Configurable expansion and collapse behavior
- Slot for user profile and notifications button